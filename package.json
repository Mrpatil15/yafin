{
  "name": "yafin",
  "version": "1.0.0",
  "description": "YAFIN - Personal Finance & Multi-Client Ledger",
  "main": "server/index.js",
  "scripts": {
    "install:all": "npm --prefix server install && npm --prefix client install",
    "build": "npm --prefix client run build",
    "start": "node server/index.js"
  }
}
